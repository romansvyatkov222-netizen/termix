// Minimal Svelte config for `npm run check` (phase 3).
// The Vite/Tauri build does not use this file; it exists so svelte-check
// can resolve compiler options instead of reporting "No Svelte
// configuration found" on every component. No preprocessors needed.
export default {};
